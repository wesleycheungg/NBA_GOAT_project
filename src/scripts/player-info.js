class PlayerInfo {
    constructor(playerSelect, players) {
        this.playerSelect = playerSelect;
        this.players = players
        this.chart = document.getElementById("chartContainer")
        
    }

    setDataForGraph(currentPlayer = this.playerSelect){
        
    }

}